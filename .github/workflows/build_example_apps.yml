name: Build Example Android and iOS Apps

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Publish revision to maven local
        run:
          ./gradlew :kmp-grpc-plugin:publishToMavenLocal :kmp-grpc-core:publishToMavenLocal -Pio.github.timortel.kmp-grpc.internal.targets=JVM

      - name: Build Android app
        run: |
          cd examples/native-integration
          ./gradlew android:assembleDebug

  build-ios:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Publish revision to maven local
        run:
          ./gradlew :kmp-grpc-plugin:publishToMavenLocal :kmp-grpc-core:publishToMavenLocal -Pio.github.timortel.kmp-grpc.internal.targets=NATIVE_APPLE

      - name: Build common module
        run: |
          cd examples/native-integration
          ./gradlew common:build -PappleTargetsOnly=true

      - name: Build iOS app
        run: |
          cd examples/native-integration/iosApp
          xcodebuild -project iosApp.xcodeproj -scheme iosApp -sdk iphonesimulator -configuration Debug LLVM_TARGET_TRIPLE_OS_VERSION=ios15.6
