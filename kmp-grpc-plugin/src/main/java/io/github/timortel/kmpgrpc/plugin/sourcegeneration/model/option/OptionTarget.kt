package io.github.timortel.kmpgrpc.plugin.sourcegeneration.model.option

enum class OptionTarget {
    FILE,
    EXTENSION_RANGE,
    MESSAGE,
    FIELD,
    ONEOF,
    ENUM,
    ENUM_ENTRY,
    SERVICE,
    METHOD
}
